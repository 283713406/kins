package prepares

func RunPrepare() error {

	//if [ "$(check_installed prepare)" == "1" ];then return;fi
	//tar -xf /mnt/k8s/prepare-${arch}.tgz -C /root/;
	//cd ${src_dir};
	//make import_prepare;
	//cd /root/prepare;
	//echo ${disable_firewall}
	//bash prepare ${disable_firewall};

	return nil
}


